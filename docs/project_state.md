## Implemented Modules
```
[x]表示已完成的功能/模块，为空或者[ ]表示模块未完成
```
[x]首页入口ui设计
[x]清理微信云开发 QuickStart 示例目录（旧 `miniprogram/` 与 `cloudfunctions/quickstartFunctions`）
[x]开发者调试模式方案文档（`/docs/develop_mode.md`）
[x]首页“加入房间”按钮的后端功能
[x]定时维护云函数 `maintenanceService`：清理过期房间、房间成员、公有快照和过期命令记录
[x]用户头像云存储生命周期清理：创建 / 加入房间时上传房间临时头像，房间进入`expired`或维护清理房间数据时删除关联头像，避免长期头像库累积

## image prompt
```
前端需要生成的图片描述，提示词需要准确

对局桌面页后续需要补充/上传到云存储的素材：
1. 深色复古档案/议会桌面背景图，竖屏小程序比例，暗青黑底、铜色细线边框、轻微纸张纹理，不含文字。
2. 自由派政策牌/政策轨格子图，青蓝发光铜边徽章风，需包含空槽与已颁布两种状态，不含文字。
3. 极权派政策牌/政策轨格子图，暗红发光铜边徽章风，需包含空槽与已颁布两种状态，不含禁用中文术语或极端主义符号。
4. 总统权力图标组：调查忠诚、特别选举、政策预览、处决、胜利徽章，铜色线稿风格，透明背景。
5. 选举轨圆形进度标记，铜色空心与高亮两种状态，透明背景。
6. 玩家座位卡框素材，左/右侧竖向座位用，包含普通、当前总统候选人、总理候选人、空位四种状态，透明背景。
```
