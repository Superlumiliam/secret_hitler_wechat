## Implemented Modules
```
[x]表示已完成的功能/模块，为空或者[ ]表示模块未完成
```
[x]首页入口ui设计
[x]清理微信云开发 QuickStart 示例目录（旧 `miniprogram/` 与 `cloudfunctions/quickstartFunctions`）
[x]开发者调试模式方案文档（`/docs/develop_mode.md`）
[ ]首页“加入房间”按钮的后端功能
[x]定时维护云函数 `maintenanceService`：清理过期房间、房间成员、公有快照和过期命令记录
[ ]用户头像云存储生命周期清理：创建 / 加入房间时上传房间临时头像，房间进入`expired`或维护清理房间数据时删除关联头像，避免长期头像库累积

## image prompt
```
前端需要生成的图片描述，提示词需要准确
```
